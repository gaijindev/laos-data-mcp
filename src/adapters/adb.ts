// TODO(Phase 5): ADB Data Library CKAN adapter -> DatasetMetadata[].
// NOTE: data.adb.org sits behind a Cloudflare bot challenge; the adapter must
// detect the challenge HTML and degrade gracefully via SourceUnavailableError.
export {};
