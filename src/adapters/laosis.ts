// TODO(Phase 7): Lao Statistics Bureau (Laosis) stub adapter.
// No public REST API documented; exposes isAvailable() health ping + a static
// category catalog, and activates live calls when LAOSIS_API_KEY is set.
export {};
